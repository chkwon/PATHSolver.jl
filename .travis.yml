# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  # - linux
  - osx
julia:
  - release
  # - nightly
notifications:
  email: false
addons:
  apt_packages:
      - gfortran
# uncomment the following lines to override the default test script
#script:
 - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
 - julia -e 'Pkg.clone(pwd()); Pkg.build("PATHSolver"); Pkg.test("PATHSolver"; coverage=true)'

# script:
#     - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#     - julia -e 'Pkg.init(); Pkg.clone(pwd()); Pkg.add("ForwardDiff"); Pkg.build("PATHSolver");  Pkg.test("PATHSolver", coverage=true)'
# after_success:
#     - echo $TRAVIS_JULIA_VERSION
#     - julia -e 'cd(Pkg.dir("PATHSolver")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
